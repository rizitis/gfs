Tray Icons Reloaded is a GNOME Shell extension which bring back 
Icons to top panel, with additional features.

- It's only works with Xorg and XWayland.
- Appindicator and similar extensions are not compatible, disable it 
  (some distributions ship it by default ex. Ubuntu based).
- If you have some trouble with compatibility you can try snixembed it 
  also adds visual native menus.
- Master always targeting current GNOME stable release.
